<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="25dp"
    tools:context=".MainActivity">
   <TextView
       android:layout_width="match_parent"
       android:layout_height="match_parent"
       android:textSize="23dp"
       android:textStyle="italic"
       android:text="Underweight: BMI less than 18.5 Normal weight: BMI 18.5 to 24.9
       Overweight: BMI 25 to 29.9"
       />
    <EditText
        android:padding="20dp"
       android:layout_marginTop="180dp"
        android:id="@+id/editTextWeight"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:hint="Enter Weight (Kg)"
        android:inputType="numberDecimal" />

    <EditText
        android:padding="20dp"
        android:id="@+id/editTextHeightFeet"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/editTextWeight"
        android:layout_marginBottom="16dp"
        android:hint="Enter Height (feet)"

        android:inputType="number" />

    <EditText
        android:padding="20dp"
        android:id="@+id/editTextHeightInches"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/editTextHeightFeet"
        android:layout_marginBottom="16dp"
        android:hint="Enter Height (inches)"
        android:inputType="number" />

    <Button
        android:id="@+id/buttonCalculate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/editTextHeightInches"
        android:layout_marginBottom="12dp"
        android:text="Calculate BMI"

        />

    <TextView
        android:id="@+id/textViewResult"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/buttonCalculate"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text=""
        android:textSize="25dp" />

</RelativeLayout>
